FROM alpine:latest AS builder

WORKDIR /build

RUN echo "I was created in the builder state" > message.txt

FROM alpine:latest

WORKDIR /app

COPY --from=builder /build/message.txt .

CMD ["cat", "/app/message.txt"]
